import React, { Suspense } from "react";
import LoginAkun from "../_components/login-akun";

const Login = () => {
  return (
    <Suspense>
      <LoginAkun />
    </Suspense>
  );
};

export default Login;
